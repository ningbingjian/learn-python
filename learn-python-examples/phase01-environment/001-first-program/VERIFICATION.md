# 01-001 验证记录

[返回课程](README.md)

## 验证环境

- 日期：2026-09-07。
- 平台：Linux；命令流程使用 bash。
- 解释器：CPython 3.12.13，命令入口 `python3`。
- 第三方依赖：无。
- 编辑器：环境中的 `code --version` 返回未安装可运行编辑器，未声称完成 VS Code GUI 验证。

## 实际执行结果

下列源码均从各自文件所在目录运行，重复两次，检查标准输出完全一致、标准错误为空且退出码为 0。随后分别复制到独立临时目录再运行，确认不依赖其他步骤文件。

| 工作目录（相对本课） | 执行命令 | 标准输出（末尾有换行） | 结果 |
| --- | --- | --- | --- |
| `step01-hello/` | `python3 main.py` | `你好，Python！` | 通过 |
| `step02-change-output/` | `python3 main.py` | `我已经运行了第一个 Python 程序。` | 通过 |
| `exercises/` | `python3 solution.py` | `我能独立修改并运行 Python 程序了！` | 通过 |

上述命令的逐条中文注释见课程与练习文档；这里记录验证事实，不是需要从一个目录连续粘贴执行的脚本。

## 额外检查

| 检查 | 方法与结果 |
| --- | --- |
| 文档与源码一致 | 提取两块带 source 标记的完整代码，与对应源码逐字比较，通过 |
| 基础 shell 流程 | 在临时目录按 `pwd`、`ls`、`python3 main.py` 顺序运行，目录、文件和问候输出符合预期 |
| 保存与运行关系 | 临时目录中的磁盘文件不变时输出旧内容；写入新内容后再次运行输出新内容，通过；未模拟 GUI 缓冲区 |
| 不存在的脚本 | 执行 `python3 mian.py`，非零退出，错误含 `can't open file`，符合课程说明 |
| 扩展名不匹配 | 临时副本改名为 `main.py.txt` 后，运行 `main.py` 失败；改回名称后成功 |
| 重复执行 | 每次输出相同，没有新增业务文件，也不修改源码 |

临时实验结束后自动清理，仓库中的两版正确源码和答案未被故障实验覆盖。本课不引入测试框架，验证程序作为编写时的检查，不要求初学者学习其实现。

## 未验证范围

- macOS/Windows 的安装流程、PowerShell 命令和 GUI 操作未在对应系统实际执行。
- VS Code 编辑、自动保存、右键菜单、编码菜单和扩展安装未进行 GUI 实测；步骤按官方文档整理。
- 本机 PATH 缺失、系统权限或安装管理器冲突无法用当前单一 Linux 环境穷尽；课程给出识别方式和官方入口，不把这些场景标为通过。

本记录表示源码和上述 Linux 实验已验证，不表示读者已经完成学习或所有平台的端到端安装均验证通过。
